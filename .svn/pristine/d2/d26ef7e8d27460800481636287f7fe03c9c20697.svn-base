import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TopupTopuplogComponent } from './topup-topuplog.component';

describe('TopupTopuplogComponent', () => {
  let component: TopupTopuplogComponent;
  let fixture: ComponentFixture<TopupTopuplogComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TopupTopuplogComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TopupTopuplogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
